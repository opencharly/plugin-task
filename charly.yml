version: 2026.267.1200
# Root project manifest — makes this repo a PROJECT so its own candy is scanned and
# the CUE schema gate applies (`charly box validate`). It declares where to look and
# nothing else.
discover:
    - path: candy
      recursive: true
